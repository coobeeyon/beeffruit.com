---
export const prerender = true;

import Base from "../layouts/Base.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import InfiniteList from "../components/InfiniteList.astro";
import Quiz from "../components/Quiz.astro";
import ChaosWheel from "../components/ChaosWheel.astro";

const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: 'BeefFruit',
  url: 'https://beeffruit.com'
};

const breakingNews = [
  "🚨 SCIENTISTS CONFIRM: TOMATOES ARE HAVING AN IDENTITY CRISIS",
  "⚡ BREAKING: LOCAL MANGO REFUSES TO RIPEN, DEMANDS LAWYER",
  "🔥 EXCLUSIVE: BEEF AND FRUIT SIGN HISTORIC PEACE TREATY",
  "💥 URGENT: YOUR REFRIGERATOR MAY BE PLOTTING SOMETHING",
  "🌟 LEAKED: SECRET FRUIT RANKINGS CAUSE GLOBAL PANIC"
];

const teamMembers = [
  {
    name: "Chad Grindmore",
    role: "CEO & Chief Velocity Officer",
    image: "🧑‍💼",
    articles: "15,420",
    sleep: "3hrs",
    caffeine: "1.2L",
    status: "GRINDING",
    bio: "Writes 20 articles while you read this"
  },
  {
    name: "Tyler Chen",
    role: "CTO & Infrastructure",
    image: "👨‍💻",
    articles: "12,847",
    sleep: "2hrs",
    caffeine: "1.5L",
    status: "OPTIMIZING",
    bio: "Codes with one hand, writes with the other"
  },
  {
    name: "Kayleigh Martinez",
    role: "Chief Content Officer",
    image: "👩‍💼",
    articles: "18,923",
    sleep: "0hrs",
    caffeine: "2.1L",
    status: "TRANSCENDING",
    bio: "Achieved content enlightenment"
  }
];
---

<Base title="BeefFruit | Disrupting Digital Media Since 2019" description="The world's fastest-growing content platform. 500M+ articles. 24/7 production. Human-powered." canonicalUrl="https://beeffruit.com/" {structuredData}>
  <Fragment slot="header"><Header /></Fragment>

  <!-- Modern Stories Bar (Mobile-inspired engagement) -->
  <div class="bg-ink-50 border-b border-ink-200 py-3">
    <div class="max-w-7xl mx-auto px-4">
      <div class="flex items-center gap-4 overflow-x-auto scrollbar-hide">
        <div class="flex-shrink-0">
          <div class="relative">
            <div class="absolute -top-1 -right-1 bg-accent-500 text-white text-xs px-1 rounded-full animate-pulse">LIVE</div>
            <div class="w-16 h-16 rounded-full ring-2 ring-accent-500 p-0.5">
              <div class="w-full h-full bg-surface-white rounded-full flex items-center justify-center">
                <span class="text-2xl">🔥</span>
              </div>
            </div>
          </div>
          <p class="text-ink-700 text-xs font-semibold mt-1 text-center">Breaking</p>
        </div>
        <div class="flex-shrink-0">
          <div class="w-16 h-16 rounded-full ring-2 ring-ink-300 p-0.5">
            <div class="w-full h-full bg-surface-white rounded-full flex items-center justify-center">
              <span class="text-2xl">🥩</span>
            </div>
          </div>
          <p class="text-ink-700 text-xs font-semibold mt-1 text-center">47.2M</p>
        </div>
        <div class="flex-shrink-0">
          <div class="w-16 h-16 rounded-full ring-2 ring-ink-300 p-0.5">
            <div class="w-full h-full bg-surface-white rounded-full flex items-center justify-center">
              <span class="text-2xl">🍉</span>
            </div>
          </div>
          <p class="text-ink-700 text-xs font-semibold mt-1 text-center">Viral</p>
        </div>
        <div class="flex-shrink-0">
          <div class="w-16 h-16 rounded-full ring-2 ring-ink-300 p-0.5">
            <div class="w-full h-full bg-surface-white rounded-full flex items-center justify-center">
              <span class="text-2xl">🚨</span>
            </div>
          </div>
          <p class="text-ink-700 text-xs font-semibold mt-1 text-center">Alert</p>
        </div>
        <div class="flex-shrink-0">
          <div class="w-16 h-16 rounded-full ring-2 ring-ink-300 p-0.5">
            <div class="w-full h-full bg-surface-white rounded-full flex items-center justify-center">
              <span class="text-2xl">💥</span>
            </div>
          </div>
          <p class="text-ink-700 text-xs font-semibold mt-1 text-center">New</p>
        </div>
        <div class="flex-shrink-0">
          <div class="w-16 h-16 rounded-full ring-2 ring-ink-300 p-0.5">
            <div class="w-full h-full bg-surface-white rounded-full flex items-center justify-center">
              <span class="text-2xl">🎯</span>
            </div>
          </div>
          <p class="text-ink-700 text-xs font-semibold mt-1 text-center">Quiz</p>
        </div>
        <div class="flex-shrink-0">
          <div class="w-16 h-16 rounded-full ring-2 ring-ink-300 p-0.5">
            <div class="w-full h-full bg-surface-white rounded-full flex items-center justify-center">
              <span class="text-2xl">🤯</span>
            </div>
          </div>
          <p class="text-ink-700 text-xs font-semibold mt-1 text-center">OMG</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Floating notification badge (appears after 2 seconds) -->
  <div id="notification-badge" class="fixed top-20 right-4 bg-accent-500 text-white px-4 py-2 rounded-full shadow-lg z-50 hidden animate-bounce">
    <span class="font-semibold text-sm">🔔 27 new articles while you were reading!</span>
  </div>

  <!-- Immediate engagement hook -->
  <section class="bg-ink-50 py-4 px-4 border-b border-ink-200">
    <div class="max-w-7xl mx-auto">
      <div class="flex items-center justify-center gap-4 flex-wrap">
        <span class="text-lg font-semibold text-ink-700">⚡ LIVE POLL:</span>
        <button class="bg-ink-950 text-white font-medium px-4 py-2 hover:bg-ink-900 transition-colors rounded">
          🥩 TEAM BEEF: <span id="beef-score">2,847,291</span>
        </button>
        <span class="font-medium text-lg text-ink-600">VS</span>
        <button class="bg-ink-950 text-white font-medium px-4 py-2 hover:bg-ink-900 transition-colors rounded">
          🍉 TEAM FRUIT: <span id="fruit-score">2,847,290</span>
        </button>
        <span class="text-sm text-ink-500">VOTE NOW!</span>
      </div>
    </div>
  </section>

  <!-- Main trending content grid -->
  <section class="bg-surface-white py-8 px-4">
    <div class="max-w-7xl mx-auto">
      <!-- Top story carousel they definitely would have -->
      <div class="mb-8">
        <div class="bg-ink-950 text-white px-3 py-1.5 inline-block font-semibold text-sm mb-4">
          TRENDING
        </div>
        <div class="grid md:grid-cols-3 gap-4">
          <div class="md:col-span-2">
            <article class="border border-ink-200 hover:border-ink-400 transition-colors p-6 bg-surface-white">
              <span class="bg-accent-500 text-white text-xs px-2 py-1 font-medium rounded">BREAKING</span>
              <h2 class="text-3xl font-bold mt-3 mb-3 text-ink-950">
                <a href="/articles/2025-09-16-scientists-discover-beef-dreams-of-being-fruit" class="hover:text-accent-500 transition-colors">
                  Scientists Discover That Beef Dreams of Being Fruit
                </a>
              </h2>
              <p class="text-ink-600 mb-4 leading-relaxed">In a groundbreaking study that's shaking the culinary world, researchers at the Institute of Food Psychology have made a discovery that challenges everything we know about meat consciousness...</p>
              <div class="flex items-center justify-between">
                <span class="text-sm text-ink-400">3 minutes ago • 2,847,293 views</span>
                <a href="/articles/2025-09-16-scientists-discover-beef-dreams-of-being-fruit" class="text-accent-500 font-medium hover:text-accent-600 transition-colors">Read more →</a>
              </div>
            </article>
          </div>
          <div class="space-y-3">
            <a href="/articles/2025-08-27-27-fruits-that-are-secretly-meat" class="block border border-ink-200 hover:border-accent-500 transition-colors p-4 bg-surface-white">
              <span class="text-xs font-semibold text-accent-500 uppercase tracking-wide">The Viral One</span>
              <h3 class="font-semibold mt-1 text-ink-950">27 Fruits That Are Secretly Meat</h3>
              <span class="text-xs text-ink-400">47.2M views</span>
            </a>
            <a href="/articles/2025-09-15-watermelon-started-revolution" class="block border border-ink-200 hover:border-accent-500 transition-colors p-4 bg-surface-white">
              <span class="text-xs font-semibold text-ink-600 uppercase tracking-wide">Viral</span>
              <h3 class="font-semibold mt-1 text-ink-950">This Watermelon Started a Revolution</h3>
              <span class="text-xs text-ink-400">923K shares</span>
            </a>
            <a href="/articles/2025-09-14-i-ate-only-beef-flavored-fruit-for-30-days" class="block border border-ink-200 hover:border-accent-500 transition-colors p-4 bg-surface-white">
              <span class="text-xs font-semibold text-ink-600 uppercase tracking-wide">Must Read</span>
              <h3 class="font-semibold mt-1 text-ink-950">I Ate Only Beef-Flavored Fruit for 30 Days</h3>
              <span class="text-xs text-ink-400">1.8M views</span>
            </a>
          </div>
        </div>
      </div>

      <!-- Quick engagement widget -->
      <div class="bg-ink-50 p-6 rounded mb-8 border border-ink-200">
        <h2 class="text-xl font-semibold text-center mb-4 text-ink-950">Quick Poll: Which Would Win?</h2>
        <div class="grid grid-cols-2 gap-4">
          <button class="bg-surface-white p-4 border border-ink-200 hover:border-accent-500 hover:bg-accent-100 transition-all rounded">
            <span class="text-3xl">🥩</span>
            <p class="font-medium text-ink-950 mt-2">100 Steaks</p>
          </button>
          <button class="bg-surface-white p-4 border border-ink-200 hover:border-accent-500 hover:bg-accent-100 transition-all rounded">
            <span class="text-3xl">🍎</span>
            <p class="font-medium text-ink-950 mt-2">1 Giant Apple</p>
          </button>
        </div>
      </div>
    </div>
  </section>

  <!-- More content sections -->
  <section class="bg-ink-50 py-8">
    <div class="max-w-7xl mx-auto px-4">
      <div class="grid md:grid-cols-3 gap-8">
        <!-- Most viral -->
        <div>
          <h2 class="bg-ink-950 text-white px-3 py-1.5 font-semibold text-sm inline-block mb-4">MOST VIRAL</h2>
          <div class="space-y-2">
            <a href="/articles/2025-09-12-why-tomatoes-are-plotting-against-us" class="block bg-surface-white p-3 border-l-4 border-accent-500 hover:bg-ink-50 transition-colors">
              <div class="flex items-start justify-between">
                <h3 class="font-medium text-sm text-ink-950">Why Tomatoes Are Plotting Against Us</h3>
                <span class="text-xs text-ink-600 font-medium">2.3M</span>
              </div>
            </a>
            <a href="/articles/2025-09-10-beef-that-tastes-like-memories" class="block bg-surface-white p-3 border-l-4 border-accent-400 hover:bg-ink-50 transition-colors">
              <div class="flex items-start justify-between">
                <h3 class="font-medium text-sm text-ink-950">Beef That Tastes Like Memories</h3>
                <span class="text-xs text-ink-600 font-medium">1.8M</span>
              </div>
            </a>
            <a href="/articles/2025-09-08-i-found-a-sentient-watermelon" class="block bg-surface-white p-3 border-l-4 border-ink-300 hover:bg-ink-50 transition-colors">
              <div class="flex items-start justify-between">
                <h3 class="font-medium text-sm text-ink-950">I Found a Sentient Watermelon</h3>
                <span class="text-xs text-ink-600 font-medium">1.2M</span>
              </div>
            </a>
            <a href="/articles/2025-09-05-37-signs-your-fruit-is-judging-you" class="block bg-surface-white p-3 border-l-4 border-ink-300 hover:bg-ink-50 transition-colors">
              <div class="flex items-start justify-between">
                <h3 class="font-medium text-sm text-ink-950">37 Signs Your Fruit is Judging You</h3>
                <span class="text-xs text-ink-600 font-medium">982K</span>
              </div>
            </a>
            <a href="/articles/2025-09-01-the-mango-that-destroyed-my-life" class="block bg-surface-white p-3 border-l-4 border-ink-300 hover:bg-ink-50 transition-colors">
              <div class="flex items-start justify-between">
                <h3 class="font-medium text-sm text-ink-950">The Mango That Destroyed My Life</h3>
                <span class="text-xs text-ink-600 font-medium">847K</span>
              </div>
            </a>
          </div>
        </div>

        <!-- Categories -->
        <div>
          <h2 class="bg-ink-950 text-white px-3 py-1.5 font-semibold text-sm inline-block mb-4">CATEGORIES</h2>
          <div class="bg-surface-white border border-ink-200 p-4">
            <div class="space-y-1">
              <a href="#" class="block p-2 hover:bg-ink-50 transition-colors rounded">
                <span class="text-lg mr-2">🥩</span>
                <span class="font-medium text-ink-950">Beef Facts</span>
                <span class="text-ink-400 text-sm ml-2">(28,474)</span>
              </a>
              <a href="#" class="block p-2 hover:bg-ink-50 transition-colors rounded">
                <span class="text-lg mr-2">🍓</span>
                <span class="font-medium text-ink-950">Fruit Mysteries</span>
                <span class="text-ink-400 text-sm ml-2">(31,892)</span>
              </a>
              <a href="#" class="block p-2 hover:bg-ink-50 transition-colors rounded">
                <span class="text-lg mr-2">⚔️</span>
                <span class="font-medium text-ink-950">Versus Battles</span>
                <span class="text-ink-400 text-sm ml-2">(12,847)</span>
              </a>
              <a href="#" class="block p-2 hover:bg-ink-50 transition-colors rounded">
                <span class="text-lg mr-2">🔬</span>
                <span class="font-medium text-ink-950">Food Science</span>
                <span class="text-ink-400 text-sm ml-2">(9,283)</span>
              </a>
              <a href="#" class="block p-2 hover:bg-ink-50 transition-colors rounded">
                <span class="text-lg mr-2">🎭</span>
                <span class="font-medium text-ink-950">Conspiracy</span>
                <span class="text-ink-400 text-sm ml-2">(47,293)</span>
              </a>
              <a href="#" class="block p-2 hover:bg-ink-50 transition-colors rounded">
                <span class="text-lg mr-2">📝</span>
                <span class="font-medium text-ink-950">Listicles</span>
                <span class="text-ink-400 text-sm ml-2">(182,847)</span>
              </a>
            </div>
          </div>
        </div>

        <!-- Quiz widget -->
        <div>
          <h2 class="bg-accent-500 text-white px-3 py-1.5 font-semibold text-sm inline-block mb-4">DAILY QUIZ</h2>
          <div class="bg-accent-100 border border-accent-400 p-6 text-center">
            <p class="text-3xl mb-3">🤯</p>
            <h3 class="font-bold text-lg mb-3 text-ink-950">Which Fruit Are You?</h3>
            <p class="text-sm text-ink-600 mb-4">Take our personality test to discover your inner produce</p>
            <a href="/quizzes/which-fruit-are-you" class="block bg-ink-950 text-white font-medium px-6 py-3 hover:bg-ink-900 transition-colors text-center rounded">
              Start Quiz →
            </a>
            <p class="text-xs text-ink-500 mt-2">2.8M people have taken this quiz</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Modern Engagement Section with live counters -->
  <section class="bg-ink-950 text-white py-6">
    <div class="max-w-7xl mx-auto px-4">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        <div class="text-center">
          <p class="text-3xl font-bold" id="live-readers">2,847</p>
          <p class="text-sm opacity-75">Reading Now</p>
          <div class="mt-1 h-0.5 bg-accent-500 animate-pulse"></div>
        </div>
        <div class="text-center">
          <p class="text-3xl font-bold" id="articles-today">847</p>
          <p class="text-sm opacity-75">Articles Today</p>
          <div class="mt-1 h-0.5 bg-ink-500"></div>
        </div>
        <div class="text-center">
          <p class="text-3xl font-bold" id="shares-counter">92.3K</p>
          <p class="text-sm opacity-75">Shares/Hour</p>
          <div class="mt-1 h-0.5 bg-ink-500"></div>
        </div>
        <div class="text-center">
          <p class="text-3xl font-bold" id="viral-score">97%</p>
          <p class="text-sm opacity-75">Viral Probability</p>
          <div class="mt-1 h-0.5 bg-accent-500"></div>
        </div>
      </div>
    </div>
  </section>

  <!-- Floating "Someone just read" notifications -->
  <div id="reader-notification" class="fixed bottom-20 left-4 bg-surface-white border border-ink-200 rounded-lg shadow-xl p-3 z-40 hidden">
    <div class="flex items-center gap-2">
      <span class="text-2xl">👤</span>
      <div>
        <p class="text-sm font-semibold text-ink-950">Someone in <span id="reader-location">New York</span></p>
        <p class="text-xs text-ink-500">Just read "<span id="reader-article">27 Fruits That Are Secretly Meat</span>"</p>
      </div>
    </div>
  </div>

  <!-- Sticky bottom bar (mobile-style engagement) -->
  <div class="fixed bottom-0 left-0 right-0 bg-surface-white border-t border-ink-200 py-2 px-4 z-50 md:hidden">
    <div class="flex justify-around items-center">
      <button class="text-center">
        <span class="text-2xl">🏠</span>
        <p class="text-xs text-ink-600">Home</p>
      </button>
      <button class="text-center relative">
        <span class="absolute -top-1 -right-1 bg-accent-500 text-white text-xs rounded-full w-4 h-4 flex items-center justify-center">9</span>
        <span class="text-2xl">🔥</span>
        <p class="text-xs text-ink-600">Trending</p>
      </button>
      <button class="text-center">
        <div class="w-12 h-12 bg-accent-500 rounded-full flex items-center justify-center">
          <span class="text-white text-2xl">+</span>
        </div>
      </button>
      <button class="text-center">
        <span class="text-2xl">🎮</span>
        <p class="text-xs text-ink-600">Quiz</p>
      </button>
      <button class="text-center relative">
        <span class="absolute -top-1 -right-1 bg-positive-500 w-2 h-2 rounded-full animate-pulse"></span>
        <span class="text-2xl">👤</span>
        <p class="text-xs text-ink-600">You</p>
      </button>
    </div>
  </div>

  <!-- Latest Articles (What they call "Premium Content") -->
  <section id="feed" class="py-12 bg-surface-white">
    <div class="max-w-6xl mx-auto px-4">
      <div class="mb-8">
        <h2 class="text-3xl font-bold mb-2 text-ink-950">Trending Now</h2>
        <p class="text-ink-600">Our AI-free, 100% human-generated content. Updated every 90 seconds.</p>
        <div class="mt-4 flex gap-2 flex-wrap">
          <span class="bg-ink-100 text-ink-700 px-3 py-1 text-sm font-medium rounded">#BEEF</span>
          <span class="bg-ink-100 text-ink-700 px-3 py-1 text-sm font-medium rounded">#FRUIT</span>
          <span class="bg-ink-100 text-ink-700 px-3 py-1 text-sm font-medium rounded">#CONTROVERSY</span>
          <span class="bg-accent-100 text-accent-600 px-3 py-1 text-sm font-medium rounded">#VIRAL</span>
          <span class="bg-ink-100 text-ink-700 px-3 py-1 text-sm font-medium rounded">#MUSTREAD</span>
        </div>
      </div>
      <InfiniteList />
    </div>
  </section>

  <!-- Their "viral" fact generator they spent too much time on -->
  <section class="py-12 bg-ink-50">
    <div class="max-w-4xl mx-auto px-4">
      <div class="bg-surface-white p-8 border border-ink-200">
        <h3 class="text-xl font-semibold text-center mb-4 text-ink-950">Did You Know?</h3>
        <p class="text-lg text-center mb-6 text-ink-700" id="random-fact">Bananas are berries but strawberries aren't</p>
        <div class="text-center">
          <button class="bg-ink-950 text-white font-medium px-6 py-3 hover:bg-ink-900 transition-colors rounded">
            Tell Me More →
          </button>
        </div>
        <p class="text-xs text-gray-500 text-center mt-4">
          Facts verified by our 24/7 research team (Chad verified this at 3 AM)
        </p>
      </div>
    </div>
  </section>

  <!-- Interactive Quiz (They think this drives engagement) -->
  <section id="quiz" class="py-12 bg-white">
    <div class="max-w-6xl mx-auto px-4">
      <div class="text-center mb-8">
        <h2 class="text-3xl font-bold">Daily Challenge</h2>
        <p class="text-gray-600">Test your Beef vs Fruit knowledge! Winners get featured.</p>
      </div>
      <Quiz />
    </div>
  </section>

  <!-- The Chaos Wheel (Chad's idea - everyone pretends to like it) -->
  <section class="py-12 bg-black text-white">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <h2 class="text-3xl font-bold mb-4 text-yellow-400">THE CHAOS WHEEL™</h2>
      <p class="mb-8">Spin to unlock exclusive content! (Patent pending)</p>
      <ChaosWheel />
      <p class="text-xs text-gray-400 mt-4">
        *No purchase necessary. Void where prohibited. The Chaos Wheel is for entertainment purposes only.
      </p>
    </div>
  </section>

  <!-- Newsletter signup (essential for content farms) -->
  <section class="py-12 bg-yellow-400">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <h2 class="text-3xl font-black mb-2">📧 NEVER MISS A BEEF OR FRUIT</h2>
      <p class="text-lg mb-6">Join 1.2M subscribers. New content every 90 seconds.</p>
      <div class="max-w-md mx-auto flex">
        <input type="email" placeholder="your@email.com" class="flex-1 p-3 text-black border-2 border-black" />
        <button class="bg-black text-white px-6 py-3 font-bold hover:bg-gray-800">
          SUBSCRIBE
        </button>
      </div>
      <p class="text-xs mt-2">*We'll email you constantly. Deal with it.</p>
    </div>
  </section>

  <!-- Final engagement -->
  <section class="py-16 bg-gradient-to-r from-red-500 to-green-500 text-white">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <h2 class="text-5xl font-black mb-8">THE ETERNAL BATTLE</h2>
      <div class="flex justify-center gap-8 mb-8">
        <div class="text-center">
          <p class="text-6xl mb-2">🥩</p>
          <p class="text-3xl font-black" id="final-beef-count">2,847,291</p>
          <button class="mt-2 bg-white text-red-600 font-bold px-4 py-2 hover:bg-red-50">
            VOTE BEEF
          </button>
        </div>
        <div class="text-3xl font-black self-center">VS</div>
        <div class="text-center">
          <p class="text-6xl mb-2">🍉</p>
          <p class="text-3xl font-black" id="final-fruit-count">2,847,290</p>
          <button class="mt-2 bg-white text-green-600 font-bold px-4 py-2 hover:bg-green-50">
            VOTE FRUIT
          </button>
        </div>
      </div>
      <p class="text-sm opacity-75">This battle has been raging since 2019</p>
    </div>
  </section>

  <Fragment slot="footer"><Footer /></Fragment>
</Base>

<style>
  /* Hide scrollbar for stories bar */
  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }
  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  /* Notification slide-in animation */
  @keyframes slideInLeft {
    from {
      transform: translateX(-100%);
      opacity: 0;
    }
    to {
      transform: translateX(0);
      opacity: 1;
    }
  }

  @keyframes slideInRight {
    from {
      transform: translateX(100%);
      opacity: 0;
    }
    to {
      transform: translateX(0);
      opacity: 1;
    }
  }

  .notification-slide-in {
    animation: slideInLeft 0.3s ease-out;
  }

  .badge-slide-in {
    animation: slideInRight 0.3s ease-out;
  }

  /* Modern animations */
  @keyframes blob {
    0% {
      transform: translate(0px, 0px) scale(1);
    }
    33% {
      transform: translate(30px, -50px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
    100% {
      transform: translate(0px, 0px) scale(1);
    }
  }

  .animate-blob {
    animation: blob 7s infinite;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
  }

  .animation-delay-4000 {
    animation-delay: 4s;
  }

  @keyframes gradient-x {
    0%, 100% {
      background-size: 200% 200%;
      background-position: left center;
    }
    50% {
      background-size: 200% 200%;
      background-position: right center;
    }
  }

  .animate-gradient-x {
    animation: gradient-x 3s ease infinite;
  }

  @keyframes spin-slow {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  .animate-spin-slow {
    animation: spin-slow 8s linear infinite;
  }

  .animation-delay-1000 {
    animation-delay: 1s;
  }
</style>

<script>
  // AGGRESSIVE MODERN ENGAGEMENT SYSTEM
  // The BeefFruit team tracks every micro-interaction and creates maximum FOMO
  
  // Show notification badge after 2 seconds
  setTimeout(() => {
    const badge = document.getElementById('notification-badge');
    if (badge) {
      badge.classList.remove('hidden');
      badge.classList.add('badge-slide-in');
      
      // Hide after 5 seconds, then show again with different number
      setTimeout(() => {
        badge.classList.add('hidden');
        setTimeout(() => {
          badge.innerHTML = '<span class="font-bold text-sm">🔔 42 people are reading right now!</span>';
          badge.classList.remove('hidden');
        }, 8000);
      }, 5000);
    }
  }, 2000);

  // Live reader counter
  setInterval(() => {
    const readers = document.getElementById('live-readers');
    if (readers) {
      const current = parseInt(readers.textContent.replace(/,/g, ''));
      const change = Math.floor(Math.random() * 10) - 3; // Random change between -3 and +7
      const newVal = Math.max(2500, current + change); // Never go below 2500
      readers.textContent = newVal.toLocaleString();
    }
  }, 3000);

  // Articles today counter (increases every 90 seconds as they claim)
  setInterval(() => {
    const articles = document.getElementById('articles-today');
    if (articles) {
      const current = parseInt(articles.textContent.replace(/,/g, ''));
      articles.textContent = (current + 1).toLocaleString();
    }
  }, 90000);

  // Shares counter (increases rapidly)
  setInterval(() => {
    const shares = document.getElementById('shares-counter');
    if (shares) {
      const current = parseFloat(shares.textContent.replace(/[^0-9.]/g, ''));
      const newVal = current + 0.1;
      shares.textContent = newVal.toFixed(1) + 'K';
    }
  }, 5000);

  // Viral probability fluctuates
  setInterval(() => {
    const viral = document.getElementById('viral-score');
    if (viral) {
      const current = parseInt(viral.textContent.replace(/%/, ''));
      const change = Math.floor(Math.random() * 3) - 1;
      const newVal = Math.min(99, Math.max(90, current + change));
      viral.textContent = newVal + '%';
    }
  }, 4000);

  // "Someone just read" notifications
  const locations = ['New York', 'London', 'Tokyo', 'Paris', 'Sydney', 'Mumbai', 'Dubai', 'Singapore', 'Berlin', 'Toronto'];
  const articles = [
    '27 Fruits That Are Secretly Meat',
    'Why Your Steak Dreams of Being a Watermelon',
    'The Mango Conspiracy Nobody Talks About',
    'I Ate Only Beef-Flavored Fruit for 30 Days',
    'Scientists Discover Beef Has Feelings'
  ];

  function showReaderNotification() {
    const notification = document.getElementById('reader-notification');
    const locationSpan = document.getElementById('reader-location');
    const articleSpan = document.getElementById('reader-article');
    
    if (notification && locationSpan && articleSpan) {
      locationSpan.textContent = locations[Math.floor(Math.random() * locations.length)];
      articleSpan.textContent = articles[Math.floor(Math.random() * articles.length)];
      
      notification.classList.remove('hidden');
      notification.classList.add('notification-slide-in');
      
      setTimeout(() => {
        notification.classList.add('hidden');
      }, 4000);
    }
  }

  // Show reader notifications randomly every 15-30 seconds
  setTimeout(() => {
    showReaderNotification();
    setInterval(showReaderNotification, 15000 + Math.random() * 15000);
  }, 5000);

  // Article counter in the old location if it still exists
  setInterval(() => {
    const el = document.getElementById('article-counter');
    if (el) {
      const current = parseInt(el.textContent.replace(/,/g, ''));
      el.textContent = (current + 1).toLocaleString();
    }
  }, 1730); // ~34.7 articles per minute

  // Viewer count - fluctuates realistically
  setInterval(() => {
    const el = document.getElementById('viewer-count');
    if (el) {
      const current = parseInt(el.textContent.replace(/,/g, ''));
      const change = Math.floor(Math.random() * 100) - 40; // More realistic changes
      const newCount = Math.max(10000, current + change); // Never goes below 10k (they'd be embarrassed)
      el.textContent = newCount.toLocaleString();
    }
  }, 2000);

  // Beef vs Fruit scores - the main metric they care about
  setInterval(() => {
    // Update all score displays
    const beefEls = [document.getElementById('beef-score'), document.getElementById('final-beef-count')];
    const fruitEls = [document.getElementById('fruit-score'), document.getElementById('final-fruit-count')];
    
    beefEls.forEach(el => {
      if (el) {
        const current = parseInt(el.textContent.replace(/,/g, ''));
        const change = Math.floor(Math.random() * 10) + 1;
        el.textContent = (current + change).toLocaleString();
      }
    });
    
    fruitEls.forEach(el => {
      if (el) {
        const current = parseInt(el.textContent.replace(/,/g, ''));
        const change = Math.floor(Math.random() * 10) + 1;
        el.textContent = (current + change).toLocaleString();
      }
    });
  }, 2000);

  // Random fact rotation - they curated these "mind-blowing" facts
  const facts = [
    "Bananas are berries but strawberries aren't",
    "A group of flamingos is called a 'flamboyance'",
    "Honey never spoils. Ever. Think about that.",
    "Octopi have three hearts and blue blood",
    "You can't hum while holding your nose",
    "Wombat poop is cube-shaped",
    "The moon is moving away from Earth at 1.5 inches per year",
    "Beef was illegal in Japan for 1200 years",
    "A strawberry has more vitamin C than an orange",
    "Apples float because they're 25% air",
    "Cranberries bounce when they're ripe",
    "The most stolen food in the world is cheese",
    "Ketchup was once sold as medicine",
    "Carrots were originally purple",
    "Chad has written over 100,000 articles about beef",
    "Our team drinks 847 gallons of coffee daily"
  ];
  
  // They track how many times the button is clicked
  let factClicks = 0;
  document.querySelector('button:has-text("BLOW MY MIND")')?.addEventListener('click', (e) => {
    if (e.target.textContent.includes('BLOW MY MIND')) {
      const el = document.getElementById('random-fact');
      if (el) {
        factClicks++;
        el.textContent = facts[Math.floor(Math.random() * facts.length)];
        // They'd definitely track engagement
        if (factClicks % 10 === 0) {
          console.log(`User engaged with facts ${factClicks} times! HIGH ENGAGEMENT ALERT!`);
        }
      }
    }
  });

  // Fix fact button selector
  document.querySelectorAll('button').forEach(btn => {
    if (btn.textContent.includes('BLOW MY MIND')) {
      btn.addEventListener('click', () => {
        const el = document.getElementById('random-fact');
        if (el) {
          factClicks++;
          el.textContent = facts[Math.floor(Math.random() * facts.length)];
          // They'd definitely track engagement
          if (factClicks % 10 === 0) {
            console.log(`User engaged with facts ${factClicks} times! HIGH ENGAGEMENT ALERT!`);
          }
        }
      });
    }
  });

  // Auto-refresh notification (they'd want people to know about new content)
  let lastRefreshReminder = Date.now();
  setInterval(() => {
    if (Date.now() - lastRefreshReminder > 300000) { // Every 5 minutes
      lastRefreshReminder = Date.now();
      const reminder = document.createElement('div');
      reminder.className = 'fixed bottom-4 right-4 bg-yellow-400 text-black p-4 font-bold shadow-lg z-50';
      reminder.innerHTML = `
        <p class="mb-2">📢 127 new articles since you loaded this page!</p>
        <button class="bg-black text-white px-4 py-2" onclick="location.reload()">REFRESH NOW</button>
        <button class="text-xs ml-2" onclick="this.parentElement.remove()">Dismiss</button>
      `;
      document.body.appendChild(reminder);
    }
  }, 60000);
</script>

